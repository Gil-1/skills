interface:
  display_name: "Handle Tickets"
  short_description: "Gate ready-ticket PRs on local and GitHub Codex review"
  default_prompt: "Use $handle-tickets to deliver existing implementation-ready tracker tickets through implementation, fresh code review, local Codex preflight and fixes, PR publication, and authoritative GitHub Codex validation."
