interface:
  display_name: "Worktree PR Review"
  short_description: "Publish a worktree PR and run Codex review"
  default_prompt: "Use $worktree-pr-review to publish the finished work from a git worktree and run Codex PR review."
